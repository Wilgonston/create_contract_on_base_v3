apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-anvil-service
spec:
  selector:
    app: {{ .Release.Name }}-anvil
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}
